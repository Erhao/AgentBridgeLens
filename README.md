# AgentBridgeLens
A Chrome extension that bridges AI coding agents with real browser environments — visual debugging, DOM inspection, source map tracing, and server-side log correlation, all from the user's actual browser.
